# Baseline mode — no Knowledge-Graph, pure mini-swe-agent (control group)
agent:
  agent_class: 'eval.agents.knowledge-graph_agent.Knowledge-GraphAgent'
  knowledge-graph_mode: 'baseline'
  step_limit: 30
  cost_limit: 3.0

environment:
  environment_class: 'docker'
